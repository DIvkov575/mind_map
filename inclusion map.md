An **inclusion map** is the natural function that embeds a subset into a larger set by sending each element to
itself.

- injective
- preserves structure

Formally, if A⊆B  the inclusion map

$$ \iota : A \hookrightarrow B$$

is defined by

$$\iota(a) = a \quad \text{for all } a \in A$$