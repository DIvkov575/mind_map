$dx = f(x, t)dt + g(x, t)dW$

- Injects brownian noise $g(x,t)dW$ into the observable, introducing stochasticity and producing a [[Diffusion process]]?
- Creates non-deterministic inference
- Captures/quantifies uncertainty
- 
