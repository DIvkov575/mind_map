**Ridge functions** are a class of functions that depend on **a linear combination of the input variables**, rather than each variable separately. Formally, a ridge function $f: \mathbb{R} \rightarrow \mathbb{R}$ has the form:
$$f(x) = g(w^\top x)$$

Constant along hyperplanes orthogonal to w
Dimensionality reduction

---
core to [[Ridge Wavelets]]