[[multilinear]] map from vectors to the underlying field
(k-form) $\omega: V \times \cdots \times V \ (\text{k times}) \to F$

eg. 1-form ([[Functional]]
$$f: V \to F, \quad f(av + bw) = a f(v) + b f(w)$$