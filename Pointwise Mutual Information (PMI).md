Pointwise [[Mutual Information]] is called _pointwise_ because it measures this association **for a single pair of events**, rather than averaging over all pairs (which is regular Mutual Information).

$M_{i,j}​=PMI(word_i​,word_j​)$
