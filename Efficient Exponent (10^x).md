
Modify exp var and baes dsimultaneously -> O(lg(x)) semi-naive process


Additinoal Tricks
  
  - Range reduction makes the input small and easy to approximate.
  - Lookup tables provide precomputed values for nearby ranges.
  - Polynomial approximation estimates the remaining small part.
  - FMA computes a*b + c with one rounding, improving speed and accuracy.
  - Hardware instructions handle scaling by powers of two and floating-point operations efficiently.


